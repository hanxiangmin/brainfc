# 验证记录 · 0.2.0

2026-09-12，Windows / Python 3.12.14。

- Python：44 项通过；Ruff 通过。原有数值、文件格式和错误边界测试保持通过。
- 新增：官方方案作用域、字典独立拷贝、TR 元数据优先及冲突、流程确认与可选项、滤波 Nyquist 与 FD 剔除、BOLD/T1 基础库存、CIFTI/GIFTI 分支、表头自动识别、配套文件匹配。
- 显示集合：先阈值与选择筛选，再限制数量；正负边和稳定排序、无连接情况都有检查。
- 前端：TypeScript 检查与 Vite 构建通过。

## 实际浏览器操作

使用独立 Playwright 和本机 Chrome 测试，无需用户登录态。应用内自动化运行环境此前无法初始化，因此不声称使用应用内 Browser 完成自动化。

`frontend/test-revision.cjs` 完整通过，未出现页面脚本或控制台错误：

1. Schaefer100 已有结果在新版中显示；深浅主题和摄像机正常。
2. 按真实投影坐标悬停前方 ROI，仅出现 1 个标签；后方 ROI 不弹出其名称。
3. 点击矩阵选择第 8 个脑区，阈值 0.2 时的 16 条连接，在三维与八视图中的 ID 逐条相同。
4. 清除选择恢复 200 条连接；PNG 下载和相同选择的 SVG 响应成功。
5. 三步引导禁止提前进入后续步骤；输入的合成 BOLD 为 TR=2 s，ADNI 3 Basic 参考为 3 s，采用扫描值并保留差异说明。
6. 同一个既有输入自动匹配此前确认的图谱和空间，方案页不需要输入文本参数。选择保留上游去噪后，处理成功并保存确认记录。
7. 生成的 file:// 离线报告也能计算同步八视图。
8. 390 px 宽度无水平溢出。

任务 `c8c64e6ca2bc48249d7991ab2e7da099` 使用**合成 BOLD 作为流程测试**，其中 ADNI 标签只是测试预设选择，不能解释成真实 ADNI 数据。证据：`.work/browser-qa-v02/validation.json`、截图和导出。

`frontend/test-workflow-branches.cjs` 完整通过：

- 一次选择 `.1D` 文件，无须输入路径；自动识别无表头时序并保留全部 60 帧、3 个 ROI。
- 没有坐标时不要求体积分区图谱，只生成矩阵。
- DICOM 转换、fMRIPrep 和质控步骤按顺序锁定；即使声明外部处理完成，仍需选择 derivatives 并确认报告。

对应合成时序任务 `28a4a985974d4a8faa8c0612363974ee`，证据 `.work/browser-qa-v02/branches.json`。

## 未完成的外部运行

这次没有新增真实患者验证，也没有在本机完成 DICOM→fMRIPrep 全链路。真实 ADHD 文件读取与提取范围、已验证和未验证的格式，保留在 [0.1.0 记录](validation.md)。

## 安装包

0.2.0 wheel / sdist 完成构建和 twine 元数据检查；独立目录安装 wheel 后核对 Python 导入、预设目录和静态界面资产。发布物校验值记录在 `dist/build-manifest-v0.2.0.json`。本地安装包可用，未发布 PyPI。
