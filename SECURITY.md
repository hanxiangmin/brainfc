# 安全与数据边界

本应用是单机工具。默认只监听 127.0.0.1，检查 Host 与 Origin，但没有用户账户/认证体系。保持回环访问，不把它直接部署为公开的多用户文件服务。同一个工作目录只运行一个服务实例。

应用可以读取运行账号权限内的本地文件；上传和结果保存在工作目录。导出报告包含源路径和校验值。NumPy 文件使用 allow_pickle=False，外部命令通过 argv 和 shell=False 调用。

如发现安全问题，使用发布仓库的私有漏洞报告功能（维护者启用后）。没有私有渠道时，公开 issue 只描述非敏感概要并请求私下联系，不附受限数据、账号信息或可直接利用的细节。

当前项目处于 0.x 开发阶段，修复优先提供于最新版本。
